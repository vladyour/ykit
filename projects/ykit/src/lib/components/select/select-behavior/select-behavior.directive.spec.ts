import { SelectBehaviorDirective } from './select-behavior.directive';

describe('SelectBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
